+_my_field_repo=https://github.com/yt-dlp/yt-dlp.git
+conflicts="yt-dlp>=0"
+depends+=" python3-yt-dlp-ejs"
+depends=${depends%python3-yt-dlp-ejs*}
+hostmakedepends+=" git"
+pkgname+=-git
+replaces="yt-dlp>=0"
+unset checksum distfiles wrksrc
+
-		_req_ejs="$(grep -o '"yt-dlp-ejs==.*"' "$wrksrc/pyproject.toml" | sed -e 's/^.*==\(.*\)"$/\1/')"
+		_req_ejs="$(grep -o '"yt-dlp-ejs==.*"' "$wrksrc/pyproject.toml" | head -n1 | sed -e 's/^.*==\(.*\)"$/\1/')"
-}
-
-post_install() {
-	cat <<- "EOF" > youtube-dl
-	#!/bin/sh
-	exec yt-dlp --compat-options youtube-dl "$@"
-	EOF
-	vbin youtube-dl
-}
-
-youtube-dl_package() {
-	depends="${sourcepkg}>=${version}_${revision}"
-	short_desc+=" (transitional compatibility package)"
-	pkg_install() {
-		vmove usr/bin/youtube-dl
-	}
-}
-
-python3-youtube-dl_package() {
-	depends="${sourcepkg}>=${version}_${revision}"
-	short_desc+=" (transitional dummy package)"
-	metapackage=yes
