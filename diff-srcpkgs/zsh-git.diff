-makedepends="gdbm-devel pcre2-devel libcap-devel ncurses-devel"
+makedepends="gdbm-devel pcre-devel libcap-devel ncurses-devel"
-homepage="https://www.zsh.org"
+homepage="http://www.zsh.org"
-if [ "$CROSS_BUILD" ]; then
-	configure_args+=" PCRE_CONFIG=$XBPS_CROSS_BASE/usr/bin/pcre2-config"
-fi
+#_my_field_repo=https://git.code.sf.net/p/zsh/code
+_my_field_repo=git://git.code.sf.net/p/zsh/code
+conflicts="zsh>=0"
+hostmakedepends+=" git"
+hostmakedepends=${hostmakedepends/texinfo/}
+make_build_target=${make_build_target/info/}
+make_install_args=${make_install_args/install.info/}
+pkgname+=-git
+replaces="zsh>=0"
+unset checksum distfiles wrksrc
-	vsed -i -e 's,/etc/service,/var/service,g' Completion/Unix/Command/_runit
+	#vsed -i -e 's,/etc/z,/etc/zsh/z,g' \
+	#	Doc/zsh.texi Doc/{zsh,zshall,zshoptions,zshmodules}.1
-	vsed -i -e 's,/etc/z,/etc/zsh/z,g' \
-		Doc/zsh.texi Doc/{zsh,zshall,zshoptions,zshmodules}.1
-
-	# "Workaround" failing tests
-	case "$XBPS_TARGET_MACHINE" in
-		x86_64-musl)
-			rm -f Test/A03quoting.ztst
-			rm -f Test/B03print.ztst
-			rm -f Test/E02xtrace.ztst
-			rm -f Test/V09datetime.ztst
-			;;
-	esac
-}
-
-pre_configure() {
-	autoreconf -fi
+	# Skip docs
+	echo '
+all: ;
+dvi: ;
+everything: ;
+help.txt: ;
+html: ;
+info: ;
+install.html: ;
+install.info: ;
+install.man: ;
+install.runhelp: ;
+install: ;
+man: ;
+pdf: ;
+ps: ;
+runhelp: ;
+texi: ;
+uninstall.html: ;
+uninstall.info: ;
+uninstall.man: ;
+uninstall.runhelp: ;
+uninstall: ;
+zsh.info: ;
+' > Doc/Makefile.in
+	./Util/preconfig
